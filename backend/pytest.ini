[pytest]
pythonpath = .
asyncio_mode = auto

# Custom markers — prevents PytestUnknownMarkWarning
markers =
    unit: fast in-process unit / integration tests (no browser)
    e2e: end-to-end browser tests via Playwright (require running servers)

# Default Playwright browser (can be overridden with --browser flag)
# playwright_browser = chromium
